class UserInfoAccount {

  constructor( {userLogin : userLogin, userPassword : userPassword} ) {
    this.userLogin = userLogin;
    this.userPassword = userPassword;
  }
};
module.exports = UserInfoAccount;
